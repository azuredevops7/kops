apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydep11
  namespace: dev
  labels:
    app: cycle1
spec:
  replicas: 7
  selector:
    matchLabels:
      app: cycle1
  template:
    metadata:
      labels:
        app: cycle1
    spec:
      containers:
        - name: cont1
          image: shammu101/game
          ports:
            - containerPort: 80
